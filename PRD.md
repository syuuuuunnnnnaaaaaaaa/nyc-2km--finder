# 🗽 NYC 2km Spot & Route Finder - PRD (Product Requirements Document)

## 1. 프로젝트 개요 (Overview)
- **서비스명**: NYC 2km Spot & Route Finder (뉴욕 반경 2km 명소 및 동선 추천 서비스)
- **목적**: 사용자가 뉴욕 내 가고 싶은 여행지를 입력하면, 해당 지점을 기준으로 반경 2km 이내의 대표 명소 3곳을 가까운 순으로 추출하고 최적의 이동 동선을 추천하여 효율적인 도보/대중교통 여행 계획을 지원함.
- **타겟 사용자**: 뉴욕 여행을 계획 중이거나 현지에서 특정 장소 주변을 탐색하고자 하는 여행자.

---

## 2. 핵심 요구사항 (Core Requirements)

### 2.1 여행지 입력 및 검색
- **입력 언어**: 한국어 및 영어 지원 (예: `타임스퀘어`, `Times Square`, `센트럴파크`, `Central Park South` 등).
- **입력 글자 수 제한**: **최소 2자 이상, 최대 50자 이하**.
- **Placeholder**: `"가고싶은 여행지를 입력하시오"`
- **요약 버튼**:
  - 기본 상태: `"요약"` 버튼 텍스트 표시.
  - 클릭 시: 버튼 내 스피너와 함께 `"검색 중"` 로딩 표시로 즉시 전환.
  - 응답 완료 시: 로딩 해제 후 결과 영역 렌더링.

### 2.2 장소 검증 및 반경 2km 명소 검색
- **장소 검증**: 입력된 장소가 뉴욕(NYC 5개 자치구) 내 유효한 위치인지 검증.
- **명소 탐색 (3곳)**:
  - 기준 위치로부터 **반경 2km(2000m) 이내**에 위치한 유명 명소 추출.
  - **가까운 순서(거리 오름차순)**로 정확히 **3개**의 명소 카드 제시.
  - 각 카드 정보: 명소명(한/영), 기준 위치로부터의 거리(km), 예상 도보 이동 시간(분), 카테고리 태그.

### 2.3 최적 이동 동선 생성
- **동선 계산**: 기준 위치(출발지) → 명소 1 → 명소 2 → 명소 3 간의 이동 순서 및 세부 구간 거리/시간 계산.
- **총 이동 요약**: 총 이동 거리(km) 및 총 예상 소요 시간(분) 표시.
- **동선 UI**: 직관적인 타임라인/스텝 뷰로 표시.

---

## 3. 예외 및 에러 처리 기준 (Error Handling & Edge Cases)

모든 에러 상황에서는 **빨간 글씨로 `"다시 입력해주세요.."`** 단일 표준 에러 문구를 표시하여 사용자에게 재입력을 유도함.

1. **입력값 길이 제한 (Too Short / Too Long)**
   - 2자 미만이거나 50자 초과인 경우 요청을 즉시 차단하고, 입력창 하단에 빨간 글씨로 `"다시 입력해주세요.."` 표시.
2. **응답 지연 및 타임아웃 (Timeout 5s)**
   - '요약' 버튼 클릭 후 서버/외부 API 응답이 **5초 이상** 지연될 경우 요청을 강제 중단(Abort)하고, 로딩 해제 후 빨간 글씨로 `"다시 입력해주세요.."` 표시.
3. **존재하지 않는 장소 및 검증 실패**
   - 뉴욕 내에서 장소를 식별할 수 없거나 위치 좌표 변환 실패 시 빨간 글씨로 `"다시 입력해주세요.."` 표시.
4. **잘못된 결과 반환 및 재시도 (Invalid Result & Retry)**
   - 반경 2km 이내 명소 검색이 불가하거나 데이터 무결성 검증 실패 시, 잘못된 결과 화면을 노출하지 않고 즉시 차단 후 빨간 글씨로 `"다시 입력해주세요.."` 표시.
   - 사용자는 횟수 제한 없이 올바른 장소를 다시 입력하거나 동일 장소를 재입력하여 재시도 가능.

---

## 4. 화면 명세 (UI Specification)

1. **헤더 영역**: 서비스 로고 및 타이틀 (`NYC 2km Spot & Route Finder`), 테마/서브 타이틀.
2. **입력창 영역**:
   - `Input`: Placeholder `"가고싶은 여행지를 입력하시오"`, 글자수 카운터 (`현재/50`), 2~50자 유효성 검사.
   - `Button`: `"요약"` (로딩 중 스피너 및 비활성화 처리).
   - `Error Text`: 빨간색 에러 문구 (`"다시 입력해주세요.."`) - 에러 발생 시 노출.
   - `Quick Chips (Demo)`: 주요 랜드마크 원클릭 테스트 칩 제공.
3. **결과 영역**:
   - **상단 섹션**:
     - 헤더 문구: `"반경 2키로 이내 유명 명소입니다"`
     - 명소 카드 3개 그리드 (순위 뱃지, 이름, 거리, 소요시간, 카테고리 태그).
   - **하단 섹션**:
     - 헤더 문구: `"추천하는 이동 동선"`
     - 동선 타임라인: 출발지 → 1차 경유지 → 2차 경유지 → 3차 도착지 시각화, 총 거리 및 총 소요 시간 메트릭.

---

## 5. 기술 스택 (Tech Stack)
- **Framework**: Next.js 16 (App Router), React 19, TypeScript
- **Styling**: Tailwind CSS v4, Lucide React 아이콘, Tailwind Animate
- **UI Components**: Base UI / Shadcn 기반 커스텀 컴포넌트
- **State/Data**: React Hooks, AbortController (5s Timeout), Geocoding/Places Engine
